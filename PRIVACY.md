# Career Quest 数据处理说明

本文件描述当前代码的实际行为，供部署方制定正式隐私政策时参考。它不是法律意见。

## 浏览器中的数据

- 界面语言保存在 localStorage。
- 未提交答案、表单、当前任务和当前评分保存在当前标签页的 sessionStorage，用于刷新恢复。
- 密码、Session ID 和 CSRF Token 不会由应用脚本写入 localStorage 或 sessionStorage。
- 登出或删除账号时会清除当前账号的标签页训练状态。

## 服务器中的数据

服务器会保存：

- 用户名、BCrypt 密码哈希、XP 和创建时间
- 生成的任务、要求、约束、验收标准和生成来源
- 用户答案、异步任务状态、幂等信息和时间
- Rubric 分数、权重、反馈、亮点、改进项、参考结构和评分来源

服务器不会在账号导出中包含密码哈希。

## AI 服务

local 模式不会把任务或答案发送给外部模型。

langchain4j 模式会把生成任务所需的输入和待评分答案发送给部署方配置的 OpenAI 兼容服务。部署方必须根据所选供应商、地区和业务场景说明：

- 接收方与处理目的
- 数据保存和训练政策
- 跨境传输
- 敏感信息限制
- 用户同意或其他合法处理基础

用户不应在训练答案中粘贴生产密钥、客户数据或其他不必要的敏感信息。

## 导出与删除

登录用户可通过界面或 GET /api/account/export 下载账号、任务、答案与评分数据。

登录用户可通过界面或 DELETE /api/account 删除账号及关联数据。共享旧版任务在仍被其他用户历史引用时会保留并安全转移归属。

数据库备份、日志副本和第三方 AI 服务中的数据是否同步删除，取决于部署方的基础设施与供应商政策；正式隐私政策必须明确说明。

## 日志与指标

应用记录运行状态、异常类型和任务标识，用于诊断。当前代码不会主动把密码写入日志。部署方仍应：

- 对访问日志和异常采集做敏感字段脱敏
- 限制日志与监控平台访问
- 设置日志、追踪和备份保存期限
- 对导出的用户数据文件进行访问控制和及时清理

## 部署方上线清单

- 使用 HTTPS，并设置 SESSION_COOKIE_SECURE=true
- 为数据库、备份、日志和导出文件制定保存与删除周期
- 确认 AI 供应商的数据使用条款
- 提供隐私联系渠道、适用地区权利说明和事件响应流程
- 多实例部署时使用受控的共享 Session、限流和配额基础设施
- 旧账号迁移结束后清空 APP_LEGACY_CLAIM_SECRET
